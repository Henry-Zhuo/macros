# key-function pairs in dictionary named "macro" in these modules
# will be used as macros
# Macros from modules later in the list can override those earlier in the list
macro_module_list = [
    'date_time_write_macro',
]
